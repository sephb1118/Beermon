<%= form_for ([@beer, @beer.comments.build]) do |f| %>
  <p>
  	<%= f.label :commenter %> <br>
  	<%= f.text_field :commenter %>
  </p>  
  <p>
  	<%= f.label :beerment %><br>
	<%= f.text_area :body %>
  </p>  
  <p>
  	<%= f.submit "Join The Discussion", class: "button" %>
  </p>
<% end %>